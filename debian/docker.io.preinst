#!/bin/sh
set -e

for conffile in \
	bash_completion.d/docker \
	default/docker \
	init.d/docker \
	init/docker \
; do
	dpkg-maintscript-helper mv_conffile \
		/etc/"$conffile".io /etc/"$conffile" 1.0.0~dfsg1-1 -- "$@"
done

#DEBHELPER#
